# Dachs MQTT Bridge for Home Assistant

This add-on reads values from SenerTec Dachs via HTTP and publishes them to MQTT.
It includes:
- MQTT Discovery
- Sector-based grouping
- Automatic entity classification (sensor, binary_sensor, switch, number)
- SCADA-friendly device grouping
